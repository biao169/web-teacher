# 本版验证范围

验证日期：2026-09-09。

- 既有回归：171 项通过，覆盖配对、文件清单、目录、个人额度、VPN 保护、恢复、维护与教师集成模块。
- 共用后台/前台组件交互：48 项通过（Happy DOM，不是视觉浏览器）。
- 独立 Vite 生产构建通过；Cloudflare Wrangler dry-run 打包通过。
- Node 和 Cloudflare 本地运行时均完成：初始化只能执行一次、来源与 CSRF 拒绝、成员管理权限隔离、中继字节一致性、4.4 MB 暂存文件、上传与下载检查点恢复、分享撤销与清理、退出登录、云端额度阻断。
- 安装脚本通过 Bash 语法检查；未在真实 Ubuntu/Debian VPS 执行 apt、证书签发或 systemd 部署。

Cloudflare 测试使用 Wrangler 本地的 DO/R2 模拟实例，没有向真实 Cloudflare 账户发布。测试容器的网卡枚举不可用，运行时验证使用仅限该测试进程的回环网卡模拟；交付的部署源码不含此适配。

跨网络直连、实际 VPN 流量、Cloudflare 线上套餐限制、域名证书签发须在部署环境验收。不要把本地运行通过理解为所有网络条件下都可直连。
