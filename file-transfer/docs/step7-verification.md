# 第 7 步验证记录

日期：2026-09-08。工具 0.7.0，Node 24.19.0，pnpm 11.19.0，数据库 schema 6。

| 检查 | 结果 |
| --- | --- |
| Node 服务、身份、文件、协议、额度与采集测试 | 122 项通过 |
| 实际 Vue 组件交互（happy-dom） | 35 项通过 |
| 教师生产包 HTTP／WebSocket 与独立服务 | 27 项通过 |
| 教师前台回归 | 74 项通过 |
| 教师及独立工具 Vue 类型 | 通过 |
| Nuxt 模块启用／停用 | 通过 |
| Ubuntu 生产构建、现有环境冻结依赖离线安装 | 通过 |
| 教师源码完整性 | 1993 个文件与第六步一致，新增改动 0；4 份受保护文档一致 |

新增覆盖包括：GB／GiB 逐字节及 64 位边界换算、严格模式拒绝虚构硬限额、统计源／账期校验、旧快照不刷新有效期、计数器重置后的人工恢复、初始共享用量、跨日／月窗口、双向及开销预留、已有窗口恰好占满额度仍可消费、新的共享流量撤销旧授权、配置版本及失联保护、重启保留待对账用量、人工对账前置条件、两个 SQLite 连接争抢最后额度、schema 5→6 保留已有个人账本及设置。

实际读取本机 Linux 接口计数，没有为测量额外生成网络流量。实际 Node 字节复制 100001 字节并逐字节比对；检查读取前预扣、配额不足不读取内容、采集失败中止协作写入。Windows 只验证固定程序与字面参数适配；本地导入脚本在独立临时目录中实际执行，并校验原子快照及数据库更新。

Vue 检查新增出口看板、精确单位往返、校准请求、活动窗口禁止对账、确认条件、草稿禁用及退出清除。修复了管理页在已有权限会话下进入时未立即读取配置的问题。实际宿主联调确认匿名／普通用户不能读取出口管理，写请求仍需同源及 CSRF；公开统计不含接口、源 ID 和管理员审计；VPN 耗尽不阻断已核验 LAN；旧版本和不合法校准／未确认对账不改变观测用量。

本版为周期采样与保守预算，不是供应商硬限额。已发出预算人工核对前不自动退回，可能与观测值暂时重复计入。严格模式始终声明 `hardLimitAvailable:false` 并保持计费链路禁用。受控传输适配器尚未接入公共上传、下载或 TURN，后续执行第 8 步。

未执行真实浏览器引擎、Windows 实机、两台物理设备 ICE、手机浏览器、多 GiB 磁盘、实际 VPN 账单／出口断流或公网部署验收。没有用内存 RTC、happy-dom 或受控 I/O 测试替代这些结论。

交付证据包包含最终通过日志、教师源码完整性和解压源码校验；不包含真实运行数据库、Cookie、私钥、用户文件。构建沿用教师工程既有的 esbuild/oxc 优先级提示及构建性能提示，未影响成功结果。
