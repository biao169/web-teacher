# 第 1 步验证记录

日期：2026-09-07。版本：0.1.0。环境：Linux、Node 24.19.0、pnpm 11.19.0。

| 检查 | 结果 |
| --- | --- |
| 工具自动化测试 | 23/23 通过，涵盖真实 HTTP、CLI 子进程、SQLite 和模块开关 |
| 迁移启动 | 复制到中文、空格目录，从另一个 cwd 启动，CLI 健康检查成功 |
| 重启和停止 | 数据库实例信息保留；SIGTERM 正常退出；端口释放；重复停止不报错 |
| 配置校验 | 无效端口、未知开关、配置缺失/损坏、越界目录和 Windows 设备名拒绝 |
| 数据隔离 | 独立 SQLite；拒绝其他数据库、目录符号链接和数据库硬链接 |
| 链路关闭 | 上传、下载、配对、身份、管理、TURN、信令接口拒绝，WebSocket 升级拒绝 |
| 就绪检测 | 存储不可用返回 503；存活检查和传输可用性分开 |
| Nuxt 真实加载 | 复用教师安装的 Nuxt 4.5.2，在临时项目中开启/关闭均通过 |
| 依赖锁 | pnpm 冻结锁、离线安装通过，新增 npm 依赖 0 |
| 教师基线 | 1993 个源文件/文档逐一对比一致；受保护文档 4 份保持不变 |
| 教师前台 HTTP | 复用第六轮验证脚本，当前构建对应当前源码，74 项通过 |

Nuxt 首次检查遇到测试夹具的依赖查找问题，已修正检查脚本从现有安装直接加载 Nuxt；最终开、关两种验证均通过。
源码包的最终哈希、解压运行记录见随包交付说明和独立验证包。

本步未完成／不作验收声明：真实 Windows 运行、浏览器界面、设备间文件传输、真实 LAN/VPN 计量、限速和总额度断流。
这些依赖后续步骤。本步没有部署到公网，也没有改动教师站导航或账号数据。

## 变更范围

新增的服务、配置、契约、模块、脚本、测试和说明全部位于 `file-transfer/`。
教师网站源码、配置和现有源码包未更改；测试使用隔离数据，不修改现有业务库。
临时数据库、真实配置、运行日志、node_modules 不进入源码包。

## 下一步

2/10：注册中英文快传页面，使用教师公共布局，补充导航与后台入口，并实现服务端验证身份后的工具短时授权。
实际传输、用户限速、VPN 预算均按计划的后续步骤逐步开启。
