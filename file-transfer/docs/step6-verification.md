# 第 6 步验证记录

日期：2026-09-08。工具 0.6.0，Node 24.19.0，pnpm 11.19.0，数据库 schema 5。

| 检查 | 结果 |
| --- | --- |
| 服务、配置、身份、文件、协议、SQLite 与控制器 | 101 项通过 |
| 实际 Vue 组件交互（happy-dom） | 30 项通过 |
| 教师生产包 HTTP／WebSocket 与独立服务联调 | 24 项通过 |
| 教师前台回归 | 74 项通过 |
| 教师站类型、独立工具 Vue 类型 | 通过 |
| 实际 Nuxt 模块开启／关闭 | 通过 |
| Ubuntu 生产构建、冻结依赖离线安装 | 通过 |
| 教师源码完整性 | 1993 个文件与第五步一致，新增改动 0；4 份受保护文档一致 |

关键新增覆盖：匿名签名篡改／过期、Cookie 的作用域与属性、匿名同源校验与登录 CSRF 校验分流、匿名拒绝后台、发送与接收独立授权、两个工作线程竞争最后额度、个人覆盖优先级、双方和任务速率取低、跨登录会话共用额度及并发、日／月不足、零额度、超过安全整数的大数、夏令时、跨午夜预留、同账号发送接收双计量、取消与重启不误退款、schema 4→5 保留原配置。

真实宿主联调发现并修复了匿名请求错误套用登录会话 CSRF 保护的问题。修复位于工具自己的桥接层，分别调用教师站已有匿名／会话校验；教师鉴权源码未改。修复后重新构建生产包，24 项宿主检查全部通过。

通过实际控制器＋真实 WebSocket 服务，使用可检查的 RTC／磁盘适配器完成 80,001 字节文件传输、内容比对和双方额度结算。接收端等待服务确认结束后才宣告任务完成。

计量为整项授权额度，不是 VPN 实际消耗。未执行浏览器引擎、Windows 实机、两台物理设备 ICE、手机浏览器、VPN 出口或多 GiB 磁盘验收；部署检查见 `access-allowance.md` 与 `lan-transfer.md`。未部署公网服务。

原始通过日志和源码包校验结果单独放入本步验收证据包；运行中的真实数据库、Cookie、密钥和临时文件不进入源码包。
