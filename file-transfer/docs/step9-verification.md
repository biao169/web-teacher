# 第 9 步验证记录

日期：2026-09-08。工具 0.9.0，Node 24.19.0，pnpm 11.19.0，数据库 schema 8。

| 检查 | 结果 |
| --- | --- |
| Node 服务、文件、协议、身份、额度、网络与恢复测试 | 159 项通过 |
| 实际 Vue 组件交互（happy-dom） | 48 项通过 |
| 教师生产包 HTTP／WebSocket 与独立服务 | 32 项通过 |
| 教师前台 HTTP 回归 | 74 项通过 |
| 教师及独立工具 Vue 类型 | 通过 |
| Nuxt 模块启用／停用 | 通过 |
| Ubuntu 生产构建、冻结依赖离线安装 | 通过 |
| 教师源码完整性 | 1993 个文件与第 2 步接入后一致，本步新增修改 0；4 份受保护文档一致 |

新增 20 项 Node 检查覆盖：持久任务／身份与方向绑定／期限；恢复重用原个人授权及原计费周期；检查点范围、事务和倒退拒绝；schema 7 → 8 保留原权限、版本、分享及账本；清单稳定排序与前缀摘要校验；中继中断与服务重启；错误目标前缀拒绝；暂存上传及一次领取上限的下载续传；本机记录容量与身份隔离；真实 WebSocket 断连后自动重连；LAN／公网 RTC 路由重新核验；目录跳过完成文件并续写部分文件；身份错误与配置撤权；新控制器从序列化记录恢复；丢失检查点确认后重传；VPN 重传重新申请预算；取消已暂停任务使凭据失效；320 个空文件的目录传输不误触普通信令限额。

Vue 新增 7 项交互检查后台恢复设置、本机记录默认关闭、暂停／继续、复用原选文件、自动重试控制、账号隔离与移除记录、速度和剩余时间展示。使用实际 Vue 组件和 happy-dom，不执行浏览器布局或原生权限窗口。

生产联调经真实教师 Nitro WebSocket 代理传输 1,400,001 字节文件，在已提交 1 MiB 检查点后暂停，双方重新获取教师授权恢复，最终字节完全一致且接收个人额度只计一次。既有真实中继、暂存、权限、CSRF 和独立页面接口测试均保持通过。教师前台 74 项覆盖原导航、登录、内容、缓存及六位密码要求。

恢复任务保持原方式，不自动回退到计费链路。个人整项授权和同一临时领取次数不重复计入；真实重传仍重新计入 VPN 预算。恢复前复核当前身份、配置版本与实际选中路径。统一设置保存后旧恢复任务失效，需要新建任务。完成记录保留至恢复期限，支持结束确认丢失后的查询；取消则撤销恢复资格。

浏览器磁盘检查点采用配置最小间隔与单文件大小 ÷ 32 的较大值，减少保留数据写入流的重复复制；本步没有对多 GiB 原生浏览器磁盘性能作测量。服务器暂存按配置间隔 fsync。仅已校验、提交并获服务器确认的数据可作为断点；未确认尾部可能重传。单文件与目录原生句柄可续写；ZIP／兼容下载在原授权中重建，128 MiB 兼容内存上限不变。

真实 WebSocket、SQLite 和服务器磁盘参与测试；RTC 候选、浏览器磁盘和刷新后的控制器使用可检查适配器，不冒充真实浏览器刷新、物理公网连接或操作系统实测。后台挂起、Windows、手机、双机 ICE、实际 VPN 供应商账单、出口硬截断及公网部署均未验收，列入第 10 步。默认严格模式仍不开放缺少硬出口保护的计费链路。

本步无新增运行依赖，仍为 MIT 的 ws 与 qrcode-generator。最终交付另附压缩包解压、离线冻结安装和从其他工作目录运行全部测试的记录，以及 SOURCE-MANIFEST.json 文件哈希清单。证据包只保留最终通过日志和完整性记录，不包含实际运行配置、数据库、密钥、会话或用户文件。

生产构建完成，保留教师工程既有的构建性能及 esbuild／oxc 提示。使用方法、默认值和升级／回滚步骤见 [断点恢复](recovery.md)。
