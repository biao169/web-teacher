# 第 3/10 步验证记录

日期：2026-09-07。版本：0.3.0 / schema 3。所有测试使用隔离数据；没有连接真实 VPN、没有部署公网服务。

| 验证 | 结果 |
| --- | --- |
| Node 单元与服务集成 | 42/42 通过，包括原 32 项回归和配置、迁移、精确单位、并发冲突、正文签名、授权撤销等新增场景 |
| 实际 Vue 组件交互（happy-dom） | 13/13 通过：保存失败保留编辑、冲突、轮询/断线、错误定位、默认草稿、账号切换清除、双视图及语言切换保留准备项 |
| 真实教师构建 + 独立工具 HTTP 联调 | 18/18 检查组通过：真实导航、身份、CSRF、同源、管理写入、冲突、策略回显、服务故障隔离 |
| 教师前台回归 | 74/74 通过 |
| 教师及独立工具界面类型检查 | 均通过；新增脚本显式覆盖宿主目录之外的工具 Vue/TS 文件 |
| 真实 Nuxt 模块开关加载 | 通过；关闭无接入，开启注册 3 页面；中文/空格宿主路径 |
| Node/Linux 生产构建 | 通过；沿用原受锁保护的构建流程 |
| 教师源码完整性 | 1993 个文件与第二步交付状态逐一一致，本步新增修改 0；01–04 保护文档未变 |

复现命令见 README。完整日志在第三步 evidence.zip，源码包不含运行数据库、真实配置、私钥、node_modules 或构建缓存。

构建前发现旧构建锁；核对原 PID 不存在且锁已过期后将其归档，再运行正常构建。未修改或绕过教师构建锁实现。

组件测试真实编译并挂载 Vue SFC，通过 DOM 输入/点击验证状态；不是截图验收。窄屏 CSS 使用可换行操作区、minmax(0,1fr) 网格和小屏单列，样式限制为 ft-*。本轮没有运行真实浏览器或布局引擎、Windows 实机、跨局域网/广域网/VPN 传输；这些仍是后续验收项，不能把本记录视为已经完成文件传输验收。
