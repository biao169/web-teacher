# 第 4/10 步验证记录

日期：2026-09-07。版本 0.4.0，数据库保持 schema 3。全部使用隔离数据和本机测试，没有部署公网或使用真实 VPN。

| 检查 | 结果 |
| --- | --- |
| 工具单元/服务集成 | 61/61 通过；包含前 42 项回归及 19 项文件/目录/保存测试 |
| ZIP 互操作 | 普通 ZIP 与 ZIP64 经 Python zipfile 读取，内容、空文件、空目录和 CRC 正确 |
| 大文件与有界流 | 超 4 GiB 元数据/ZIP64 标头、不读全文预检、分块读取、背压、大小错误、取消与内存累计上限通过 |
| 实际 Vue 组件交互 | 20/20 通过；包括原管理页回归、File input、预览、语言/视图保留、错误不丢选择、真实 ZIP Blob、逐批清单 |
| 真实宿主/工具 HTTP 联调 | 18/18 检查组通过；导航、页面文件区域、身份、CSRF、配置保存/冲突、能力状态与故障隔离 |
| 教师前台 HTTP 回归 | 74/74 通过 |
| 工具独立目录和教师类型检查 | 通过 |
| 真实 Nuxt 模块开关及生产构建 | 通过 |
| 教师源码 | 1993 个源码/文档与第三步状态逐一一致，本步新增教师源码修改 0；01–04 保护文档未变 |

使用原有 Node 24.19.0 / Vue 3.5.42 / Nuxt 4.5.2，无新增 npm 依赖。测试 Python 3 用于独立 ZIP 解码，不是应用运行依赖。命令见 README。

构建前核验旧锁原 PID 不存在且时间已过期，归档后运行原受锁保护的构建流程；未更改教师构建锁代码。

DOM 测试真实编译/挂载 Vue，但未执行真实浏览器布局或授权窗口。目录写入使用句柄替身；文件/ZIP 内容使用实际 File/Blob 字节和独立解码。未执行 Windows 实机、多 GiB 浏览器磁盘写入、LAN/WAN/VPN 网络传输、真实浏览器截图验收，不能将本步结果描述为跨设备快传已完成。
