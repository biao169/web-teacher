# 测试记录（可随本文件夹一起删除）

验证日期：2026-09-08。环境：Linux，Node 24.19.0，pnpm 11.19.0；复用现有已安装依赖。

本次目录调整复测：根目录 Tweb.sh Bash 语法与管理测试13项通过，包含密码两次输入、初始化令牌保留/删除、快传角色授权、一句下载命令语法及下载失败不执行安装。示例启动器已移入 support/ 后复测9项通过、1项夹具跳过。以下业务全量与生产构建/HTTP结果沿用上一版：本次未修改两个应用的业务源码。

| 项目 | 结果 |
| --- | --- |
| 快传 `node --test --test-concurrency=1 tests/*.test.mjs` | 171 通过，0 失败 |
| Windows 启动器 Node 回归 | 9 通过；1 项需专用已有教师数据库夹具而跳过 |
| 当前生产管理回归 | 13 通过：语法/help、真实本地非main分支克隆、数据保护、停机/备份/迁移顺序、权限/恢复、交互初始化与远程下载失败保护 |
| 启用快传的 Nuxt Ubuntu 生产构建 | 通过；存在构建工具性能/配置提示，无构建失败 |
| 生产进程 HTTP | `/health`、`/zh`、`/en`、`/zh/transfer`、`/en/transfer`、首页 API 均 200 |
| 教师→快传身份桥 | capabilities 返回 200，protocolVersion=1，identityBridgeAvailable=true |
| 源码包 | 排除依赖、生产输出、真实数据库/上传/密钥；逐文件 SHA-256 与 ZIP CRC 验证 |

部署测试使用隔离临时目录及模拟 systemctl 流程，没有在本环境创建系统账号、安装 systemd 服务、执行 apt、修改真实 VPN/防火墙/Nginx。HTTP 测试通过本机端口运行真实生产进程，不代表 TLS 或登录后的完整浏览器验收。

尚需目标机验收：干净 Ubuntu/Debian 上依赖下载和 apt/systemd 完整安装；Windows 双击/符号链接权限；实际 Git 分支访问权限与供应商网络；管理员首次初始化；HTTPS证书与反代 WebSocket；真实 LAN/WAN/VPN双机传输、限速、配额及恢复演练。

完整备份与恢复脚本用于生产管理，但本记录不是所有目标机环境均已验证的承诺。首次上线请在空白测试服务器演练安装、更新、备份恢复后再迁入真实数据。
