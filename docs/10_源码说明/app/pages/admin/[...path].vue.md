# app / pages / admin / [...path].vue

## 文件定位

- **源码路径**：`app/pages/admin/[...path].vue`
- **文件类型**：页面
- **功能定位**：后台页面入口，对应路由 /admin/:path*；负责装配后台布局、工作区或状态页。
- **规模**：16 行，827 字节
- **内容校验**：SHA-256 `2015281e3c9d831b73ebdf83928f0cc59418325ecad38470ab3b939fe50e6bb7`
- **页面路由**：`/admin/:path*`

## 直接依赖

- `~~/shared/admin/content-modules`
- `~~/shared/admin/registry`

## 方法与函数

| 名称 | 类型/位置 | 用途 | 用法 |
| --- | --- | --- | --- |
| `contentRoute` | computed 声明，第 6 行 | 派生 content Route 的响应式状态，供模板和交互逻辑读取 | 仅在本文件内部使用，标识符共出现 3 次。 |
| `selected` | computed 声明，第 7 行 | 派生 selected 的响应式状态，供模板和交互逻辑读取 | 仅在本文件内部使用，标识符共出现 3 次。 |
| `title` | 对象函数，第 9 行 | 封装 title 相关逻辑，供本文件或上层模块按其参数调用 | 仅在本文件内部使用，标识符共出现 3 次。 |

### 调用签名

- `contentRoute`：`contentRoute = computed(() => adminContentRoute(route.path))`
- `selected`：`selected = computed(() => adminModuleForPath(route.path))`
- `title`：`title: () => contentRoute.value?.definition.title ?? selected.value?.title ?? '后台页面'`

## Vue 模板交互

- 模板约 5 行；样式区约 0 行。
- 子组件：`AdminContentWorkspace`、`AdminModulePlaceholder`、`AdminStatePanel`
- 事件绑定：无显式事件绑定。
- 动态属性：`route-model`、`module`

## 维护注意事项

- 页面文件只负责路由装配；可复用业务状态应进入 composable，展示细节应进入 component。
