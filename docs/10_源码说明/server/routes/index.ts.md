# server / routes / index.ts

## 文件定位

- **源码路径**：`server/routes/index.ts`
- 功能：根地址的动态默认语言入口，替代固定跳转 /zh 的 routeRules。
- 规模：9 行，507 字节
- SHA-256：`68fb6447234f0ce4a294089703bb1a930cccbed00e02b00f4a434ff3b1d45632`

## 方法与用法

| 方法/数据 | 用途和使用方式 |
| --- | --- |
| `默认事件处理器` | 只接受 GET 和 HEAD；设置 private/no-store 与 Vary，再判断语言并 302 跳转到 /zh 或 /en，保留原查询字符串。 |
| `边界` | 仅处理 /；显式双语地址、后台、API、媒体与静态文件均不参与地区跳转。此路由不要求数据库、密钥或登录状态。 |

## 维护边界

详细配置和验收见 `docs/18_IP默认语言与手动偏好验收.md`。语言规则集中于共享工具，可信代理策略复用现有安全模块，不在各页面独立复制判定。
