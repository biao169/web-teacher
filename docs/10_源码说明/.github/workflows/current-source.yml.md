# .github / workflows / current-source.yml

## 文件定位

- **源码路径**：`.github/workflows/current-source.yml`
- **文件类型**：配置/数据
- **功能定位**：持续集成工作流；在代码提交或拉取请求时执行项目校验。
- **规模**：41 行，1287 字节
- **内容校验**：SHA-256 `3b1077baa0a9a8692d099315911beb72e2f9c8cc6a18a8d91dedc33afc9d370e`

## 方法与函数

该文件没有独立的命名函数或类方法；其行为由声明式配置、模板、SQL、样式规则或框架默认入口构成。

## 配置结构

顶层键：`on`、`permissions`、`concurrency`、`jobs`。修改时应通过对应工具的 Schema 或现有验证命令确认。

## 维护注意事项

- 修改本文件前先检查直接调用方和共享契约，避免只修单一路径而破坏另一运行时。
