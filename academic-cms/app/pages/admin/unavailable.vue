<script setup lang="ts">import { RefreshCw } from '@lucide/vue'; import { ElButton } from 'element-plus'; import { safeAdminReturnPath } from '~~/shared/admin/paths'; definePageMeta({ layout: 'admin' }); useSeoMeta({ title: '后台服务暂不可用' }); const route = useRoute(); const target = computed(() => safeAdminReturnPath(route.query.next, '/admin'))</script>
<template><AdminStatePanel tone="unavailable" title="后台身份服务暂不可用" description="当前无法确认登录状态。系统没有继续请求任何后台业务数据。"><template #actions><ElButton type="primary" tag="a" :href="target"><RefreshCw :size="16" />重新尝试</ElButton></template></AdminStatePanel></template>
