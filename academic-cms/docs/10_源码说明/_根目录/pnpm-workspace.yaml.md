# pnpm-workspace.yaml

## 文件定位

- **源码路径**：`pnpm-workspace.yaml`
- **文件类型**：配置/数据
- **功能定位**：项目支持文件；为工程运行、依赖锁定或开发工具提供配置。
- **规模**：20 行，325 字节
- **内容校验**：SHA-256 `2cbc68eb42b859d75f39937e6311053cccc0478591c061e7416fa45aadaec196`

## 方法与函数

该文件没有独立的命名函数或类方法；其行为由声明式配置、模板、SQL、样式规则或框架默认入口构成。

## 配置结构

顶层键：`packages`、`allowBuilds`、`overrides`。修改时应通过对应工具的 Schema 或现有验证命令确认。

## 维护注意事项

- 修改本文件前先检查直接调用方和共享契约，避免只修单一路径而破坏另一运行时。
