# db / adapters / d1.ts

## 文件定位

- **源码路径**：`db/adapters/d1.ts`
- **文件类型**：程序模块
- **功能定位**：平台适配器；屏蔽 Node、Cloudflare 或数据库驱动差异，向上层提供统一接口。
- **规模**：58 行，2970 字节
- **内容校验**：SHA-256 `309bedd359c3d1123d1ea20686d10193780a60aac6ef8ae40986b4da13476775`

## 直接依赖

- `../contracts`
- `../errors`
- `../statement`

## 直接调用方

- `db/runtime/cloudflare.ts`
- `tests/workerd/repository.spec.ts`

## 方法与函数

| 名称 | 类型/位置 | 用途 | 用法 |
| --- | --- | --- | --- |
| `normalizeResult` | 函数，第 21 行 | 规范化 Result，消除不安全或不一致的输入形式 | 仅在本文件内部使用，标识符共出现 3 次。 |
| `D1Adapter.constructor` | 构造方法，第 30 行 | 封装 constructor 相关逻辑，供本文件或上层模块按其参数调用 | 由 `db/runtime/cloudflare.ts`、`tests/workerd/repository.spec.ts` 等模块导入使用。 |
| `D1Adapter.prepare` | 类方法，第 31 行 | 封装 prepare 相关逻辑，供本文件或上层模块按其参数调用 | 由 `db/runtime/cloudflare.ts`、`tests/workerd/repository.spec.ts` 等模块导入使用。 |
| `D1Adapter.execute` | 类方法，第 37 行 | 执行 execute 所代表的完整处理流程 | 由 `db/runtime/cloudflare.ts`、`tests/workerd/repository.spec.ts` 等模块导入使用。 |
| `D1Adapter.batch` | 类方法，第 46 行 | 封装 batch 相关逻辑，供本文件或上层模块按其参数调用 | 由 `db/runtime/cloudflare.ts`、`tests/workerd/repository.spec.ts` 等模块导入使用。 |

### 调用签名

- `normalizeResult`：`function normalizeResult(result: D1ResultLike, command: SqlCommand): QueryResult`
- `D1Adapter.constructor`：`constructor(private readonly binding: D1BindingLike)`
- `D1Adapter.prepare`：`private prepare(command: SqlCommand): D1StatementLike`
- `D1Adapter.execute`：`async execute(command: SqlCommand): Promise<QueryResult>`
- `D1Adapter.batch`：`async batch(commands: readonly SqlCommand[]): Promise<QueryResult[]>`

## 类型、类与导出数据

| 名称 | 类型/位置 | 用途 |
| --- | --- | --- |
| `D1ResultLike` | 接口，第 6 行 | Accepts a D1 binding or a request-scoped D1 session. |
| `D1StatementLike` | 接口，第 12 行 | 约束 D1 Statement Like 的数据结构或可选值 |
| `D1BindingLike` | 接口，第 17 行 | 约束 D1 Binding Like 的数据结构或可选值 |
| `D1Adapter` | 类，第 27 行 | 封装 D1 Adapter 的状态与业务行为 |

## 维护注意事项

- 修改本文件前先检查直接调用方和共享契约，避免只修单一路径而破坏另一运行时。
