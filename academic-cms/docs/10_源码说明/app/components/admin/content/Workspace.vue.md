# app / components / admin / content / Workspace.vue

## 文件定位

- **源码路径**：`app/components/admin/content/Workspace.vue`
- **文件类型**：Vue 组件
- **功能定位**：根据通用内容路由切换列表或编辑器；以模块、模式和 UID 区分编辑实例，避免跨对象复用表单。
- **规模**：8 行，474 字节
- **内容校验**：SHA-256 `2756e37d66f1a8bcb575775dfdfc8c1d29250752d3b357b7689e24025814ea48`

## 接入与状态约定

根据通用内容路由切换列表或编辑器；以模块、模式和 UID 区分编辑实例，避免跨对象复用表单。

## 直接依赖

- `~~/shared/admin/content-modules`

## 方法、函数与派生状态

| 名称 | 位置 | 用途与用法 |
| --- | --- | --- |
| 测试声明 / 组件模板 | 文件主体 | 挂载或读取被测实现，按断言验证上述职责；无独立命名方法。 |

### 调用签名



## 模板接入

使用组件：`AdminContentEditor`、`AdminContentList`。

## 维护要求

- 本步行为及验收边界见 [20_后台编辑生命周期统一与验收.md](../../../../../20_后台编辑生命周期统一与验收.md)。
- 共用编辑壳必须显式导入；业务 API、expectedUpdatedAt、权限和媒体引用规则由既有服务承担。
- docs/01–04 为受保护需求基线，不随本次实现更新。
