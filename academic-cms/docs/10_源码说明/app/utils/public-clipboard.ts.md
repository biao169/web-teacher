# app/utils/public-clipboard.ts

## 文件定位

- **源码路径**：`app/utils/public-clipboard.ts`
- **功能定位**：支持即时或Promise复制载荷，在等待公开内容前同步调用富文本剪贴板写入；完整载荷通过验证后才允许纯文本降级，取消时拒绝过期后续写入，缺失能力或失败真实抛出。
- **规模**：28 行，1512 字节
- **内容校验**：SHA-256 `db2c5993fded5ace82de9c88380f15e61ec6880779b42a61791728d84705aa98`

## 使用与维护

支持即时或Promise复制载荷，在等待公开内容前同步调用富文本剪贴板写入；完整载荷通过验证后才允许纯文本降级，取消时拒绝过期后续写入，缺失能力或失败真实抛出。

命名函数：`writePublicClipboard`。

## 直接依赖

- `~~/shared/utils/public-copy`

本步说明见 `docs/39_前台改版_一键复制.md`。一键复制已在第7步实施；真实浏览器与系统剪贴板验收属于第8步。
